use anyhow::Result;

fn main() -> Result<()> {
    println!("AI Agent - CLI Interface");
    println!("This is a minimal starter for our AI agent with Anthropic Sonnet support");
    
    // We'll expand this with proper command-line parsing and functionality
    // in future iterations
    
    Ok(())
}
